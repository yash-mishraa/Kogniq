import type { EnvironmentMetadata } from "@/app/workspace/WorkspaceTypes";
import type { ReactNode } from "react";
export function WorkspaceHeader({ environment, onLeave, headerAction }: { environment: EnvironmentMetadata; onLeave?: () => void; headerAction?: ReactNode }) { return <header className="flex items-center justify-between"><button type="button" onClick={onLeave} aria-label="Choose another environment" className="w-fit text-left text-[clamp(1.45rem,2.5vw,2.3rem)] font-medium tracking-[-.05em] text-ink outline-none hover:text-accent focus-visible:text-accent">{environment.title}</button><div id="workspace-header-action">{headerAction}</div></header>; }
