# Shared Constants

Reserved for rare, immutable semantic constants shared across package contracts.

Environment values, feature decisions, secrets, mutable configuration, provider identifiers, and domain-specific values do not belong here.

