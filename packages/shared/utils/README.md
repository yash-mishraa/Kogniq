# Shared Utilities

Reserved for small, deterministic, framework-neutral utilities proven useful across multiple packages.

Business logic, package-specific helpers, mutable state, provider wrappers, and speculative convenience functions do not belong here. New utilities require named ownership and direct tests.

