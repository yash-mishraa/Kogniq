# Backend Architecture

Future audience-facing description of the implemented `apps/api` composition root, domain boundaries, application workflows, and external contracts.

Current plans live in [the API package contract](../../.ai/package_contracts.md#appsapi), [service catalog](../../.ai/service_catalog.md), and [API inventory](../../.ai/api_catalog.md). No backend framework or API exists.

